#include "tree.h"
#include <stdio.h>
#include <string.h>

//int main(int argc, char *argv[]) {
//    NODE *test;
//    memset(test, 0, sizeof(struct node));
//    test->type = 'F';
//    strncpy(test->name, "testing", strlen("testing"));
//    strncpy(test->name,  "hello", strlen("hello"));
//    printf("The node hasvalue: %s", test->name);
//    return 0;
//}
